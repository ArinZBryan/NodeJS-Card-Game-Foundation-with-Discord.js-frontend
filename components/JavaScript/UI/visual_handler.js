const game = require("../../../gameLogic")
const sharp = require('sharp')
function get_image() {
  for (let i = 0; i > game.)
    sharp('curr_iter.png')
      .composite([{input:next_image_to_overlay}])
      .toFile('new_iter.png')
}

module.exports = {get_image : get_image};

